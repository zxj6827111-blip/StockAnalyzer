# 学习链 C3/C5 五变体配对比较结果（2026-09-15）

> 预注册：`docs/learning_chain_c3_preregistration_20260915.md`（变体定义、参数、判据均在运行前钉死）。
> 口径限定：本结果全部是 **214 特征世代 / 全市场资格集 / 流程级评估**，见预注册 §5.1。
> **不是独立证据、不构成晋升依据**——选择窗口与评估窗口刻意设为同一段历史（该窗口参与过
> 标签/特征/面板的开发），工具的 verdict 因此按设计全部为 `inconclusive`。

## 1. 运行环境与可复现性

| 项 | 值 |
|---|---|
| 部署 | NAS `build_commit=28dede0801f4b49813160bdeece4601581fc5d71`（main HEAD） |
| 数据 | `artifacts/phase2_label_remediation/pit_dataset_rank`（24 分片，2,405,272 行） |
| 报告 | `artifacts/phase2_label_remediation/c3_variants/phase2_walk_forward_*.json`（每变体一份） |
| fold | 18 折，`ci_block=5`，`lookahead_violations_total=0`，`baseline_comparable=true` |
| 逐变体耗时 | blend 5m16s / raw_blend 6m / ridge 2m15s / stump 2m16s / reversal 1m30s |
| 容器内存 | 峰值 ~2.9 GiB / 4 GiB，`memory.events` 全 0（含 `oom_kill`） |

## 2. 各变体总览

| 变体 | 有效日 | 无效日 | IC 均值 | IC 95% CI | verdict |
|---|---|---|---|---|---|
| `blend`（校准后 meta） | 329 | **13** | 0.0564 | [0.0271, 0.0834] | **NO_GO** |
| `raw_blend`（校准前） | 342 | 0 | **0.0612** | [0.0281, 0.0913] | GO_CANDIDATE |
| `ridge`（线性） | 342 | 0 | 0.0306 | [0.0029, 0.0566] | NO_GO |
| `stump`（强正则单树） | 342 | 0 | 0.0336 | [0.0035, 0.0636] | GO_CANDIDATE |
| `reversal`（固定 −ret_20d） | 342 | 0 | 0.0578 | [0.0262, 0.0898] | NO_GO |

「无效日」= 当日 IC 非有限值（分数常数 → 无横截面区分度），按预注册 §3.4 单独计数、不剔除。

## 3. 与 `blend` 的配对增量（同窗口、同资格集、同脚本）

| 变体 | ΔIC 均值 | ΔIC 95% CI | 错误相关 | 失效月（ΔIC<0） | verdict |
|---|---|---|---|---|---|
| `raw_blend` | **+0.0098** | **[+0.0013, +0.0188]** | 0.943 | 6/18 | inconclusive |
| `ridge` | −0.0234 | [−0.0668, +0.0163] | −0.036 | 11/18 | inconclusive |
| `stump` | −0.0237 | [−0.0665, +0.0173] | −0.019 | 12/18 | inconclusive |
| `reversal` | +0.0007 | [−0.0386, +0.0429] | 0.164 | 11/18 | inconclusive |

按预注册 §4「ΔIC 只在配对 CI 不跨 0 且方向为正时才算有增量」：**只有 `raw_blend` 达标**
（CI 下界 +0.0013 > 0），其余三个都不成立。

## 4. C5 成本、换手与失效月（单边 10 bps，口径同 `reversal_baseline`）

| 变体 | 毛收益均值 | **净收益均值** | 平均换手 | 失效月 | 天数 |
|---|---|---|---|---|---|
| `blend` | 0.00949 | 0.00933 | 0.1630 | 5 | 342 |
| `raw_blend` | 0.00971 | **0.00952** | 0.1898 | 5 | 342 |
| `ridge` | 0.00623 | 0.00603 | 0.2004 | 6 | 342 |
| `stump` | 0.00758 | 0.00751 | 0.0728 | 5 | 342 |
| `reversal` | 0.00858 | 0.00840 | 0.1778 | 6 | 342 |

口径提醒：这里的收益是「上尾 30% 等权组合的**当日前瞻收益均值**」，**不是复利净值**，
也不含持有重叠、涨跌停不可成交、T+1。它只用于成本与换手对照。

按预注册 §4 的硬判据「跑不赢成本后的反转基线则不具备可部署性」：
`ridge`（0.00603）与 `stump`（0.00751）**低于**反转基线（0.00840）→ 两者出局；
`blend`（0.00933）与 `raw_blend`（0.00952）仅以 **+0.00093 / +0.00112 每日**（约 +11% / +13% 相对）
高于反转基线，且失效月 5 个。

## 5. 结论（按证据强度排序）

1. **校准在伤害排序，而且已被两项独立测量确认。** `raw_blend` 相对 `blend` 的 ΔIC
   CI 完全在正区间（+0.0013~+0.0188），错误相关 0.943（几乎同一信号，故差异来自校准本身）；
   且 `blend` 出现 **13 个无效日**（校准后分数塌成常数）而 `raw_blend` 为 0，
   直接导致 `blend` 的 verdict 掉到 `NO_GO`、`raw_blend` 为 `GO_CANDIDATE`。
   这与 A2 修复后仍观察到的 isotonic 塌缩（校准器唯一值 7、spread 为负）一致——
   **A2 修的是边界实现缺陷，校准器本身在当前数据上仍是负贡献。**
2. **整个 214 特征模型对单因子反转基线没有统计上可辨的增量。** `reversal` 的 IC 0.0578
   与 `blend` 的 0.0564 之差为 +0.0007，CI 横跨 0；成本后也只有每日 +0.0009 的优势。
   即「213 个特征 + 双模型集成 + 校准」相对一行 `−ret_20d` 的边际价值落在噪声里。
3. **线性与单树确实差一半**（0.031 / 0.034 vs 0.061），所以集成/非线性本身是有作用的；
   问题不在「模型太简单」，而在**特征栈相对反转因子的增量太小**——与
   `week5-feature-attribution-0907` 的归因结论方向一致。
4. 全部 `lookahead=0`、覆盖门通过、`baseline_comparable=true`，所以上面三条不是前视或口径问题造成的。

## 6. 明确不成立的推论

- **不得**据此说「模型有效」或「模型无效」：这是**开发窗口内**的相对对照，verdict 全为
  `inconclusive`；效力级的绝对判断需要批次 D 的冻结工件前向 shadow。
- **不得**据此说「214/222 特征的差异影响了结论」：两者都缺同样 8 个特征，属同世代比较。
- **不得**把 `blend` 的数字当作部署中模型的表现（部署模型走 sample-store + 222 维 schema）。
- **不得**把这里的收益均值复利成年化（口径见 §4 提醒）。

## 7. 后续动作建议（按性价比）

1. **校准去留**：把 `raw_blend` 作为默认推理分数、校准降级为诊断量——这是本次唯一 CI 显著的
   结论，且改动面小、可回滚（`output_semantics` 契约已就位，B2 输出健康门可继续守）。
2. **特征栈 vs 反转因子的边际价值**：先做特征归因（哪些特征在反转因子上仍有增量），
   再决定是否扩展特征，而不是继续加模型复杂度。
3. 本结果**不改变**「return_rank 保留研究方向、自动训练与自动晋升均关闭」的现状。
